apiVersion: rediscloud.redis.com/v1alpha1
kind: ACLUser
metadata:
  name: example-acl-user
spec:
  forProvider:
    name: app-user
    role: user
    passwordSecretRef:
      name: redis-user-password
      namespace: default
      key: password
  providerConfigRef:
    name: default